<resources>

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

    
    
    <!-- List View Button -->
    <style name="ListViewButton">
    	<item name="android:background">@drawable/list_view_button</item>
    	<item name="android:textColor">@color/light_blue</item>
    	<item name="android:layout_height">40dp</item>
    </style>
    
    <style name="GreenButton">
        <item name="android:background">@drawable/list_view_green_button</item>
        <item name="android:textColor">@color/tan</item>
        <item name="android:layout_height">50dp</item>
    </style>
    
    <style name="RedButton">
        <item name="android:background">@drawable/red_button</item>
        <item name="android:textColor">@color/tan</item>
        <item name="android:layout_height">50dp</item>
    </style>
    
</resources>
